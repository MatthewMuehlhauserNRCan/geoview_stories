/**
 * Quote panel type definition
 * Used for displaying quotes with attribution
 */
export interface QuotePanelData {
  quote: string;
  author?: string;
  role?: string;
  organization?: string;
}
